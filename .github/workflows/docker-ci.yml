name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  prod:
    if: startsWith(github.ref, 'refs/tags/')  
    uses: equinor/ops-actions/.github/workflows/docker.yml@e204ec946bafb37000a2f698a66aee30a13b2c73
    secrets:
        password: ${{ secrets.DOCKER_PASSWORD }}
    with:
      environment: prod
      registry: registry.hub.docker.com
      username: sondresjo
      repository: sondresjo/bobby-api
      tag: ${{ github.ref_name }}